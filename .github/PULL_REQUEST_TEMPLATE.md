<!--
Per docs/AI-COLLABORATION-CONVENTIONS.md §4 every PR body must include:
  §4a Files changed (always)
  §4b docs + tests verified (always — say "no docs/tests needed" if so)
  §4c Test expectations (only if some CI checks are expected to fail)
  §4d Work breakdown (always)
  §4e Operational impact (always except pure doc/test/CI-only PRs)
Closing keyword (`Closes #NNN`) is required; one issue per PR.
-->

## Summary

<!-- 1–3 bullets: what changes, why, observable outcome. -->

Closes #

## Work breakdown

<!-- Mirror the TodoWrite list used during implementation. See §4d. -->

1.
2.

## Files changed

<!-- Alphabetical by path, one line per file. See §4a. -->

- **path/to/file.py** — one-line summary.

## Docs and tests

<!-- Per §4b: confirm both checked. State "no docs/tests needed" explicitly if so. -->

- Docs updated:
- Tests updated / added:

## Operational impact

<!-- Per §4e: restart / rebuild / migration mechanics, or "No restart, rebuild, or migration needed."
     Skip the section entirely only for pure doc/test/CI-only PRs. -->

## Test expectations

<!-- Per §4c: include this section ONLY if some CI checks are expected to fail.
     Otherwise delete this section entirely. -->
